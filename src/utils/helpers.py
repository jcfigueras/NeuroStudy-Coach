"""
Small reusable utility placeholders.
Extend with date/time helpers, validation, and formatting as needed.
"""


def format_duration(minutes):
    """Stub: format minutes as human-readable duration. Implement when needed."""
    # TODO: Implement (e.g. "1h 30m").
    return f"{minutes} min"
